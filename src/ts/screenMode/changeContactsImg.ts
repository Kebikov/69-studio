import { TMode } from "./darkMode";

/**
 * Замена элементов в блоке с контактами.
 */
const changeContactsImg = () => {
    /**
     * Блок с контактами.
     */
    const contactsBlock = document.querySelector('.contacts-main');
    if(contactsBlock) {
        
    }
}

export default changeContactsImg;